# Preliminary-plan completion plan

## Dependency order

1. Task 21: produce a deterministic SQLite legacy-data upgrade/downgrade/replay
   rehearsal and redacted evidence.
2. Task 22: add a model-free Mock/real route switch rehearsal with byte-exact
   configuration rollback.
3. Task 23: bind L1/L2/L3 DetectionResult/Evidence and terminal
   ExperienceRecord outcomes to one task.
4. Task 24: run a versioned 3-5 task benchmark comparing deterministic local
   and governed AgentTeams modes without conflating historical Providers.
5. Task 25: provide and verify clean Docker one-command reproduction; the real
   recording remains Human-owned.
6. Task 26: implement/evaluate one team-original Skill and prove at least three
   real Skill calls in the main task. MiniMax or StepFun may be used if a model
   is required; the accepted Task 24 report remains immutable MiniMax history,
   and any new diagnostic uses a separate run ID with exact Provider binding.
7. Task 27: perform a requirement-by-requirement design, package, repository,
   release, and submission audit; check only directly proven items.
8. Human checkpoint: publish repository/recording, verify URLs, freeze commit,
   tag/release, and complete the competition page.

## Checkpoints

- After Tasks 21-23: full tests, migrations, event/evidence replay, Ruff, mypy,
  dependency audit, and secret scan pass.
- After Tasks 24-26: benchmark and live evidence hashes bind to the exact code,
  Provider, Skill versions, Agent identities, and ToolCalls.
- After Task 27: every remaining unchecked item is either directly proven or
  explicitly Human/external; no engineering item is hidden behind a broad claim.
